package day21;

public class Student extends Parent{
    public    String name="Ali Can";
    Integer age =12;
    private char initial='A';




    // 1)if you dont type any access modifier, its means the access modifier is default
    // 2)publis class members(variables, methods) are accessible entire the project, but default cannot be visible from other packets
    //3) private class members cannot be accessible from other classes other packets
    //   private  class members can be accessible only from the class itself
    //default class members cannot  be accessible from  other packets
    // 5) protected can be accessible from child classes in any packets





}
